// JK not going to finish this but this is it started
// CS 431
// /0 String Library

/*
Int strIndex(char s[], c)                      first occurrence of c from left

Int strrightindex(char s[], c)                         first occurrence of c from right

strAdd(char s[], t[])                                        add string t onto s

Int strupto(char s[]; int start; char rejectset[])      return first location of char in rejectset

Int strspan(char s[];  int start; char acceptset[])    return first location of char not in acceptset

Int strfind(char s[], t[])               find t in s

Int strsize(char s[])                      size of string

Int map(char s[], from[], to[])     map the characters in from with the ones in to (must be same size) in s

Replace(char s[], from[], to[])     replace string from with to throughout s

strNull()   not sure what it does but it is in your list.
*/

int strIndex(char s[], c){ //first occurrence of c from left
    int location = 0;
    
    // return int
    return location;
}

int strRightIndex(char s[], c){ //first occurrence of c from right
    int location = 0;
    
    // return int
    return location;
}

int strupto(char s[]; int start; char rejectset[]) { //return first location of char in rejectset
    int location = 0;
    
    // return int
    return location;
}

void strAdd(char s[], t[]){ // add string t onto s
   // no return
}

int strupto(char s[]; int start; char rejectset[])  { //return first location of char in rejectset
    int location = 0;
    
    // return int
    return location;
}

int strFind(char s[], t[]){ // find t in s
    int location = 0;
    
    // return int
    return location;
}

int strSize(char s[])  { // return size of string
    int size = 0;
    
    // return int
    return size;
}

int map(char s[], from[], to[]) { //map the characters in from with the ones in to (must be same size) in s
    int size = 0;
    
    // return int
    return size;
}

void replace(char s[], from[], to[]) { // replace string from with to throughout s
    
    // no return
}

void addNull(char s[]){ // add null character to the end of a string
    
    // no return
}

main()
{
    // ----- Variables -----
    int a;
    int b;
    
    // hard coded strings
    char s = "default\0";
    char a = "default";
    char v = "horses";
    char e = "dog\0";
    char b = "cat\0";
    char d = "backwards\0";
    char c = "sdrawkcab\0";
}

